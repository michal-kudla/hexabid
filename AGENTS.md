# AGENTS.md

## Reguły nazewnictwa

- Dla zmiennych/parametrów reprezentujących nagłówek `X-API-Version` używaj zapisu **`xApiVersion`**.
- Nie używaj wariantu **`xAPIVersion`**.
- Zasada dotyczy kodu pisanego ręcznie oraz plików wygenerowanych, jeśli są utrzymywane w repo.
